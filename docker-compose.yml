version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: dropbox-postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: dropbox_app
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - dropbox-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Optional: Uncomment to run the NestJS app in Docker
  # app:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: dropbox-app
  #   restart: always
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - NODE_ENV=development
  #     - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/dropbox_app?schema=public
  #   volumes:
  #     - .:/app
  #     - /app/node_modules
  #     - ./uploads:/app/uploads
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   networks:
  #     - dropbox-network
  #   command: yarn start:dev

volumes:
  postgres_data:
    driver: local

networks:
  dropbox-network:
    driver: bridge


